{
  "name": "pollution-forecast-lambda",
  "version": "1.0.0",
  "type": "module",
  "description": "AWS Lambda function to fetch pollution forecast and save to Firestore",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "functions-framework --target=handler --source=index.mjs"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "@google-cloud/functions-framework": "^3.0.0"
  }
}